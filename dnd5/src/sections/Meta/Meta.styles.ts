import styled from 'styled-components';

const Container = styled.section`
  display: inline-flex;
  justify-content: space-between;
  margin: 1em 0;
  width: 100%;
`;

export default {
  Container,
};
